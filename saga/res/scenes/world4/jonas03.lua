local hero4 = getHero(3).getName()

speak("Jonas", "The ruins we're looking for date from before the poison.")
speak(hero4, "By the rock?")
speak("Jonas", "We're looking for a hidden entrance a few blocks north of it.")

sceneSwitch('jonas3', true)
