local hero1 = getHero(0).getName()
local hero2 = getHero(1).getName()

speak("BASE DESTRUCTION SEQUENCE START. EVACUATE. EVACUATE.")

speak(hero2, "What??")
speak("Jonas", "My info must've been wrong!")
speak(hero1, "Let's get out of here!")

sceneSwitch('missile04', true)
